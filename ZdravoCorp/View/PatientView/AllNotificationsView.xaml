<UserControl x:Class="ZdravoCorp.View.PatientView.AllNotificationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ZdravoCorp.View.PatientView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid x:Name="DataGridAppointments"
                  ItemsSource="{Binding Notifications, UpdateSourceTrigger=PropertyChanged}"
                  SelectedItem="{Binding SelectedAppointment, UpdateSourceTrigger=PropertyChanged}"
                  SelectionMode="Single"
                  IsReadOnly="True" AutoGenerateColumns="False" Width="600" Height="325" Margin="100,85,100,40">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Width="300" x:Name="name" Binding="{Binding When}" />
                <DataGridTextColumn Header="Note" Width="300" Binding="{Binding Message}" />
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="New Notification" HorizontalAlignment="Left" Margin="136,36,0,0" VerticalAlignment="Top" Height="32" Width="123"/>
    </Grid>
</UserControl>
