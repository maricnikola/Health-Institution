<Window x:Class="ZdravoCorp.GUI.Scheduling.Views.ControlAppointmentView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Height="180" Width="300"
          Background="#29262e"
      Title="Control appointment">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0 30 0 0">
            <Label Content="Do you want to schedule a check-up in 10 days?" Height="70" FontSize="13" Foreground="WhiteSmoke" />
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" Margin="35 0 0 0">
                <Button Padding="7" Command="{Binding Yes}" Content="Yes" Width="100"/>
            </Grid>
            <Grid Grid.Column="1" Margin="30 0 0 0">
                <Button Padding="7" Command="{Binding No}" Content="No" Width="100"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
